spring:
  application:
    name: family-clan-backend
  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3306/family_clan?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
        type:
          preferred_uuid_jdbc_type: VARCHAR
    open-in-view: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: buixuanhienmy@gmail.com
    password: xgkqbnuesocfljyg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# Application specific configurations
app:
  jwt:
    secret: 9a2f6c8d1e3f5b7a9c8e7d6c5b4a3f2e1d0c9b8a7f6e5d4c3b2a1f0e9d8c7b6a
    # Expiration: 1 day in milliseconds (24 * 60 * 60 * 1000 = 86400000)
    accessTokenExpirationMs: 86400000
    # Refresh Expiration: 7 days in milliseconds (7 * 24 * 60 * 60 * 1000 = 604800000)
    refreshTokenExpirationMs: 604800000
    # Reset Password Expiration: 15 minutes in milliseconds (15 * 60 * 1000 = 900000)
    resetTokenExpirationMs: 900000
  upload:
    dir:
      public: ./uploads/public
      private: ./uploads/private
